Don't forget to include *--recurse-submodules* flag while cloning:
```
git clone --recurse-submodules git@github.com:yildirimyigit/use_case_crf.git
```

After cloning, build and run docker with:

- ``` ./build_docker.sh ```
- ``` ./run_gui.sh ```
